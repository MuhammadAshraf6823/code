function change(){

let fnum = document.getElementById("fnumber").value;
let snum = document.getElementById("snumber").value;
let fsnum = parseInt(fnum) + parseInt(snum)
document.write(fsnum)
}

function change1(){

    let fnum = document.getElementById("fnumber").value;
    let snum = document.getElementById("snumber").value;
    let fsnum = parseInt(fnum) - parseInt(snum)
    document.write(fsnum)
    }

    function change2(){

        let fnum = document.getElementById("fnumber").value;
        let snum = document.getElementById("snumber").value;
        let fsnum = parseInt(fnum) * parseInt(snum)
        document.write(fsnum)
        }
    